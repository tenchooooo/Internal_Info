<h2>予定</h2>
<p>出欠勤情報</p>
<p>終日休み</p>
<table border="1" class= 'p-top__introduce'>
  <tr>
    <th>日時</th>
    <th>部署名</th>
    <th>名前</th>
  </tr>
<% @schedules.each do |schedule| %>
  <tr>
    <% if schedule.member_id == @member.id %>
    <% if schedule.oneday_off? %>
    <td><%= schedule.day %></td>
    <td><%= schedule.member.department %></td>
    <td><%= schedule.member.last_name %></td>
    <td><%= button_to "削除", schedule_path(schedule.id), method: :delete %></td>
    <% end %>
    <% end %>
  </tr>
<% end %>
</table>


<p>午前休み</p>
<table border="1" class= 'p-top__introduce'>
  <tr>
    <th>日時</th>
    <th>部署名</th>
    <th>名前</th>
  </tr>
<% @schedules.each do |schedule| %>
  <tr>
    <% if schedule.member_id == @member.id %>
    <% if schedule.morning_off? %>
    <td><%= schedule.day %></td>
    <td><%= schedule.member.department %></td>
    <td><%= schedule.member.last_name %></td>
    <td><%= button_to "削除", schedule_path(schedule.id), method: :delete %></td>
    <% end %>
    <% end %>
  </tr>
<% end %>
</table>

<p>午後休み</p>
<table border="1" class= 'p-top__introduce'>
  <tr>
    <th>日時</th>
    <th>部署名</th>
    <th>名前</th>
  </tr>
  <% @schedules.each do |schedule| %>
  <tr>
    <% if schedule.afternoon_off? %>
    <% if schedule.member_id == @member.id %>
    <td><%= schedule.day %></td>
    <td><%= schedule.member.department %></td>
    <td><%= schedule.member.last_name %></td>
    <td><%= button_to "削除", schedule_path(schedule.id), method: :delete %></td>
    <% end %>
    <% end %>
  </tr>
  <% end %>
</table>


<p>出張情報</p>
<table border="1" class= 'p-top__introduce'>
  <tr>
    <th>日時</th>
    <th>部署名</th>
    <th>名前</th>
    <th>行先</th>
  </tr>
  <% @schedules.each do |schedule| %>
  <tr>
    <% if schedule.trip? %>
    <% if schedule.member_id == @member.id %>
    <td><%= schedule.day %></td>
    <td><%= schedule.member.department %></td>
    <td><%= schedule.member.last_name %></td>
    <td><%= schedule.trip %></td>
    <td><%= button_to "削除", schedule_path(schedule.id), method: :delete %></td>
    <% end %>
    <% end %>
  </tr>
<% end %>
</table>


<p>外出情報</p>
<table border="1" class= 'p-top__introduce'>
  <tr>
    <th>日時</th>
    <th>部署名</th>
    <th>名前</th>
    <th>行先</th>
  </tr>
  <% @schedules.each do |schedule| %>
  <tr>
    <% if schedule.go_out? %>
    <% if schedule.member_id == @member.id %>
    <td><%= schedule.day %></td>
    <td><%= schedule.member.department %></td>
    <td><%= schedule.member.last_name %></td>
    <td><%= schedule.go_out %></td>
    <td><%= button_to "削除", schedule_path(schedule.id), method: :delete %></td>
    <% end %>
    <% end %>
  </tr>
  <% end %>
</table>


<p>その他情報</p>
<table border="1" class= 'p-top__introduce'>
  <tr>
    <th>日時</th>
    <th>部署名</th>
    <th>名前</th>
    <th>内容</th>
  </tr>
  <% @schedules.each do |schedule| %>
  <tr>
    <% if schedule.other? %>
    <% if schedule.member_id == @member.id %>
    <td><%= schedule.day %></td>
    <td><%= schedule.member.department %></td>
    <td><%= schedule.member.last_name %></td>
    <td><%= schedule.other %></td>
    <td><%= button_to "削除", schedule_path(schedule.id), method: :delete %></td>
    <% end %>
    <% end %>
  </tr>
  <% end %>
</table>